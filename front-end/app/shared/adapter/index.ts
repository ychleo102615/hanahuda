/**
 * Shared Kernel - Adapters
 *
 * @description
 * 跨 BC 共用的 Port 實作。
 */

export { ToastNotificationAdapter, createToastNotificationAdapter } from './ToastNotificationAdapter'
export { CurrentPlayerContextAdapter, createCurrentPlayerContextAdapter } from './CurrentPlayerContextAdapter'
